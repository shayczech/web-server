# ------------------------------------------------------------------------------
# Inventory: set ec2_instance_ip to the EC2 instance public IP (Elastic IP).
# ------------------------------------------------------------------------------

[webservers]
ec2_instance_ip

[all:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/id_rsa_runner
ansible_python_interpreter=/usr/bin/python3
ansible_become=true